# 🔬 本地 AI 编码能力评测系统方案

## 📋 项目目标

参照 [marginlab.ai Claude Code 追踪器](https://marginlab.ai/trackers/claude-code/)，搭建本地评测环境，实现：
1. **每日自动评测** AI 编码工具 (Claude Code, Cursor, Copilot 等) 的软件工程任务表现
2. **性能退化检测** 通过统计检验发现显著性能变化
3. **可视化追踪** 生成趋势图和报告

---

## 🏗️ 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                     本地评测系统架构                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │ 任务调度器   │───→│ Patch 生成器 │───→│ 评测执行器   │     │
│  │ (Cron/手动) │    │ (AI Agent)  │    │ (Docker)    │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│         │                  │                  │             │
│         ▼                  ▼                  ▼             │
│  ┌─────────────────────────────────────────────────┐       │
│  │              SWE-Bench 数据集                    │       │
│  │  (问题描述 + 代码仓库 + 预期测试结果)             │       │
│  └─────────────────────────────────────────────────┘       │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────┐       │
│  │              结果存储 & 分析                     │       │
│  │  • JSON 日志 • SQLite/PostgreSQL • 统计检验     │       │
│  └─────────────────────────────────────────────────┘       │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────┐       │
│  │              可视化 & 报告                       │       │
│  │  • 每日通过率 • 趋势图 • 退化警报               │       │
│  └─────────────────────────────────────────────────┘       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 数据集选择

| 数据集 | 任务数 | 难度 | 推荐场景 | 每轮评估时间 |
|--------|--------|------|----------|--------------|
| **SWE-bench Lite** | 300 | ⭐⭐ | 快速验证 | ~2-4h |
| **SWE-bench Verified** | 500 | ⭐⭐⭐ | 官方基准 | ~4-8h |
| **SWE-bench Pro** | 731 | ⭐⭐⭐⭐ | 深度评测 | ~8-16h |
| **自定义子集** | 50-100 | 可调 | **日常追踪 (推荐)** | ~1-2h |

**建议：** 从 SWE-bench Verified 或 Pro 中抽取 **50-100 个代表性任务** 作为日常评测集，兼顾覆盖度和运行时间。

---

## 💻 硬件需求

### 最低配置
| 资源 | 需求 | 说明 |
|------|------|------|
| CPU | 8 核 | 并行运行 Docker 容器 |
| RAM | 16 GB | 评测任务内存消耗 |
| 存储 | 120 GB SSD | Docker 镜像 + 代码仓库 |
| 网络 | 稳定连接 | 调用 AI API |

### 推荐配置 (每日自动化)
| 资源 | 需求 | 说明 |
|------|------|------|
| CPU | 16+ 核 | 更高并发 |
| RAM | 32 GB | 多任务并行 |
| 存储 | 256 GB SSD | 历史数据留存 |
| 云服务 | Modal/AWS (可选) | 弹性扩展 |

---

## 🔧 软件依赖

### 核心组件
```bash
# 1. Docker (必需)
brew install docker  # macOS
# 或参考 https://docs.docker.com/engine/install/

# 2. Python 3.10+
python --version  # 确认 >= 3.10

# 3. SWE-bench 工具包
git clone https://github.com/SWE-bench/SWE-bench.git
cd SWE-bench
pip install -e .

# 4. Modal (可选，云端并行评测)
pip install modal
modal setup
```

### AI Agent 组件 (任选)
```bash
# 方案 A: Claude Code CLI (marginlab 使用的方式)
npm install -g @anthropic-ai/claude-code

# 方案 B: SWE-Agent (开源 Agent 框架)
git clone https://github.com/SWE-agent/SWE-agent.git
cd SWE-agent && pip install -e .

# 方案 C: Mini-SWE-Agent (轻量版)
pip install mini-swe-agent
```

---

## 📁 项目结构

```
eval-system/
├── config/
│   ├── config.yaml          # 主配置
│   └── models.yaml          # 模型配置 (API keys)
├── data/
│   ├── swe-bench-subset.json # 评测任务子集
│   └── results/              # 评测结果存储
├── scripts/
│   ├── run_eval.py          # 评测入口
│   ├── generate_patch.py    # Patch 生成
│   ├── analyze.py           # 统计分析
│   └── report.py            # 报告生成
├── docker/
│   └── Dockerfile           # 自定义镜像 (如需)
├── reports/
│   └── daily/               # 每日报告
└── README.md
```

---

## 🔄 评测流程详解

### Step 1: 准备评测子集
```python
from datasets import load_dataset

# 加载 SWE-bench Verified
swebench = load_dataset('princeton-nlp/SWE-bench_Verified', split='test')

# 抽取子集 (例如 50 个任务)
import random
subset = random.sample(list(swebench), 50)

# 保存为本地文件
import json
with open('data/eval_subset.json', 'w') as f:
    json.dump(subset, f)
```

### Step 2: 生成 Patch
```bash
# 使用 Claude Code CLI 生成修复
for instance in $(cat data/eval_subset.json | jq -r '.[].instance_id'); do
    claude-code solve \
        --instance $instance \
        --output patches/$instance.patch
done

# 或使用 SWE-Agent
python -m sweagent.run \
    --model claude-sonnet-4-20250514 \
    --instances data/eval_subset.json \
    --output_dir patches/
```

### Step 3: 运行评测
```bash
# SWE-bench 官方评测脚本
python -m swebench.harness.run_evaluation \
    --dataset_name princeton-nlp/SWE-bench_Verified \
    --predictions_path patches/predictions.json \
    --max_workers 8 \
    --run_id "daily-$(date +%Y%m%d)"
```

### Step 4: 统计分析
```python
import json
import numpy as np
from scipy import stats

# 加载历史数据
with open('data/results/history.json') as f:
    history = json.load(f)

# 计算基准线
baseline = np.mean([r['pass_rate'] for r in history[-30:]])

# 今日结果
today_rate = 0.54  # 示例

# 统计检验 (单样本 t 检验)
t_stat, p_value = stats.ttest_1samp(
    [r['pass_rate'] for r in history[-30:]], 
    today_rate
)

if p_value < 0.05:
    print(f"⚠️ 检测到显著性能变化! p={p_value:.4f}")
```

### Step 5: 生成报告
```python
# 生成 HTML 报告 (复用已有工具)
from reports.tools.update_index import generate_report

generate_report(
    date='2026-01-30',
    pass_rate=0.54,
    baseline=0.58,
    degradation_detected=True
)
```

---

## ⏰ 自动化调度

### Cron 任务配置
```bash
# 每天凌晨 2 点运行评测
0 2 * * * cd /path/to/eval-system && ./scripts/run_daily_eval.sh

# 或使用 Clawdbot Cron
clawdbot cron add \
    --name "daily-ai-eval" \
    --schedule "0 2 * * *" \
    --command "执行每日 AI 编码能力评测"
```

---

## 💰 成本估算

### 一次评测成本 (50 任务)

| 项目 | 消耗 | 单价 | 成本 |
|------|------|------|------|
| Claude API (Opus) | ~500K tokens | $15/M input, $75/M output | ~$10-20 |
| Claude API (Sonnet) | ~500K tokens | $3/M input, $15/M output | ~$2-5 |
| 本地计算 | 2-4h | 电费 | ~$0.5 |
| Modal 云计算 (可选) | 2h GPU | ~$2/h | ~$4 |

**每日成本：** $2-25 (取决于模型选择)
**每月成本：** $60-750

### 降低成本建议
1. **使用 Sonnet 而非 Opus** — 成本降低 ~80%
2. **缩小评测子集** — 从 50 降到 20 任务
3. **批量评测** — 非高峰时段运行
4. **缓存中间结果** — 避免重复生成

---

## 📈 可视化方案

### 选项 A: 静态 HTML (推荐起步)
- 复用已有的 `reports/tools/` 工具链
- 生成 GitHub Pages 托管的趋势图
- 使用 Chart.js 绘图

### 选项 B: Grafana + InfluxDB
- 实时数据展示
- 更丰富的交互功能
- 适合长期运营

### 选项 C: 自建 Dashboard
- Next.js + Vercel 部署
- 完全自定义 UI/UX

---

## 🚀 实施路线图

### Phase 1: 基础搭建 (1-2 天)
- [ ] 安装 Docker + SWE-bench
- [ ] 下载数据集并抽取子集
- [ ] 手动运行单次评测验证环境

### Phase 2: 自动化 (2-3 天)
- [ ] 编写 Patch 生成脚本
- [ ] 配置评测流水线
- [ ] 设置 Cron 定时任务

### Phase 3: 可视化 (1-2 天)
- [ ] 实现结果存储
- [ ] 生成趋势图
- [ ] 集成到报告系统

### Phase 4: 优化 (持续)
- [ ] 统计检验算法调优
- [ ] 降低 API 成本
- [ ] 扩展到更多模型

---

## ❓ 待确认问题

1. **评测范围**：只追踪 Claude Code，还是同时追踪 Cursor/Copilot？
2. **预算**：每月可接受的 API 成本上限？
3. **通知方式**：检测到退化时，通过什么渠道提醒？
4. **数据存储**：本地 SQLite 还是云端数据库？

---

## 📎 参考资源

- [SWE-bench 官方仓库](https://github.com/SWE-bench/SWE-bench)
- [SWE-bench Pro](https://github.com/scaleapi/SWE-bench_Pro-os)
- [SWE-Agent](https://github.com/SWE-agent/SWE-agent)
- [marginlab 追踪器](https://marginlab.ai/trackers/claude-code/)
- [Modal 云计算平台](https://modal.com/)

---

*方案版本: v1.0 | 生成时间: 2026-01-30*
